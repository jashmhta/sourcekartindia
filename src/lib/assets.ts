/**
 * Local assets only - all brand imagery is served from /public/images/brand/
 * No external CDN dependencies.
 */
export const assets = {
  videos: {
    scan: "/video/scan.webm",
    loop1: "/video/loop1.webm",
    loop2: "/video/loop2.webm",
    clouds: "/video/clouds.webm",
  },
} as const;
